<br>
<h1>Add a film to your list (bookmark).</h1>
<br>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit "Save movie in bookmark", class: "btn btn-primary" %>
<% end %>
<br>
<%= link_to 'Back', list_path(@list) %>
